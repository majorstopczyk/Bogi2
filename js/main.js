const divEl = document.createElement('div');
divEl.setAttribute('id', 'slider');

main.appendChild(divEl);

const imgEl = document.createElement('img');
imgEl.setAttribute('class', 'image');

